NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Local development header-auth context. Production uses OIDC and stores the
# short-lived access token in the browser session/local storage.
NEXT_PUBLIC_TENANT_ID=demo-tenant
NEXT_PUBLIC_ORGANIZATION_ID=demo-org
NEXT_PUBLIC_USER_ID=demo-user
NEXT_PUBLIC_ROLE=platform_admin
